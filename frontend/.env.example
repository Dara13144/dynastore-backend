# ==============================================================================
# DYNA STORE FRONTEND - ENVIRONMENT CONFIGURATION (.env)
# ==============================================================================

# For Local Development (Uses Vite reverse proxy to backend on port 5050):
VITE_API_URL="/api/v1"
VITE_SOCKET_URL="http://localhost:5050"
